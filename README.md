# Phonebook App (Demo app for phonebook)


## Install all node packages

Run the following command to install dependencies

    $ npm install
    $ bower install



Start the application with:

    $ npm start
